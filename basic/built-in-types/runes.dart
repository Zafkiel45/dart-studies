void main() {
  // RUNES =====================================================================

  // Arrived the difficult part for me...! But, here we go.
  // Runes expose the Unicode code points perceived by the user.
  // Strings are represents as code in memory, something like: \uXXXX
  // Runes expose these code.
  // expose the Unicode code is useful in situations when it is necessary
  // manipulate individual characters.
  // in summary, there is a package to handle runes and characters. More
  // about this in another file.
}
